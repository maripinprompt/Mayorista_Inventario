# nombre de la aplicacion
spring.application.name=inventario

# puerto en el que corre este microservicio
server.port=9091

# cambio clave: localhost reemplazado por host.docker.internal
# esto permite que Docker encuentre el MySQL de tu PC
spring.datasource.url=jdbc:mysql://host.docker.internal:3306/mayorista_inventario
spring.datasource.username=root
spring.datasource.password=

# credenciales de Spring Security
spring.security.user.name=admin
spring.security.user.password=12346

# configuracion de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# url del microservicio de proveedores
proveedor.service.url=http://host.docker.internal:8080